import * as assert from "assert";
import Logs from '../utilities/Logs';
require("moment");
const _ = require('lodash');
require('fs');

export default class Utilities {

    static isDisplayed (element: any) {
    }

    static async getChildElement(parentElem: any, childElem: any) {
    }

    static async waitForElementVisible(element: any, sElementName = "") {
    }

    static async setBrowserKey(sKey: any) {
    }

    static async enterText (element: any,value: string,elemName: string) { 
   }

    static async click (element: any,elemName: string, msg="") {
    }

    static async getText (element: any, elemName: string) {
    }

}
